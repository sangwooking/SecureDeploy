version: "3.9"

services:
  web:
    build: .
    image: adminhub-web:latest
    privileged: true
    network_mode: host
    environment:
      NODE_ENV: production
    ports:
      - "3000:3000"
  db:
    image: postgres:latest
    environment:
      POSTGRES_PASSWORD: adminhub-password
